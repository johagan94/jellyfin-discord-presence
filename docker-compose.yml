services:
  jellyfin-discord-presence:
    image: ghcr.io/johagan94/jellyfin-discord-presence:latest
    # Or build from source instead of pulling:
    # build: .
    container_name: jellyfin-discord-presence
    restart: unless-stopped
    env_file: .env
